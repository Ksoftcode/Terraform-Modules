# terraform-azurerm-postgresqlflexibleserver

> **Note:** This README is auto-generated. Module content unavailable (submodule not initialized).

## Overview

**Purpose:** Azure PostgreSQL Flexible Server

**Status:** ⚠️ Submodule not initialized - cannot analyze actual code

## Quick Start

```hcl
module "postgresqlflexibleserver" {
  source = "path/to/terraform-azurerm-postgresqlflexibleserver"

  # Required variables (inferred)
  project     = "myapp"
  environment = "dev"
  owner       = "team@example.com"
  location    = "eastus"

  # Module-specific variables
  # name = "..."
  # resource_group_name = "..."

  tags = {
    cost_center = "IT-12345"
  }
}
```

## Requirements

| Name | Version |
|------|---------|
| terraform | >= 1.4 (recommended) |
| azurerm | ~> 3.80 (recommended) |

## Providers

| Name | Version |
|------|---------|
| azurerm | (unknown) |

## Inputs

> ⚠️ **Cannot determine inputs** - submodule not initialized

| Name | Type | Required | Default | Description |
|------|------|----------|---------|-------------|
| project | string | yes | n/a | *Inferred:* Project name |
| environment | string | yes | n/a | *Inferred:* Environment (dev, prod) |
| owner | string | yes | n/a | *Inferred:* Owner email/team |
| location | string | no | eastus | *Inferred:* Azure region |
| name | string | no | null | *Inferred:* Resource name override |
| resource_group_name | string | conditional | n/a | *Inferred:* Resource group |
| tags | map(string) | no | {} | *Inferred:* Additional tags |

*TODO: Update with actual variables after submodule initialization*

## Outputs

> ⚠️ **Cannot determine outputs** - submodule not initialized

| Name | Description | Sensitive |
|------|-------------|-----------|
| id | *Inferred:* Resource ID | no |
| name | *Inferred:* Resource name | no |

*TODO: Update with actual outputs after submodule initialization*

## Resources Created

> ⚠️ **Cannot determine resources** - submodule not initialized

Based on module name, likely creates:
- `azurerm_*` resources related to azure postgresql flexible server

## Data Sources Used

Unknown - requires submodule initialization

## Tagging & Naming Convention

Follow organization standards:
- Use `tags` variable for additional tags
- Merge with default tags in locals
- Required tags: project, environment, owner, terraform_managed

## Backend & State Guidance

**Do NOT configure backend in this module.**

Backend configuration belongs in the root module that calls this module.

## Security Considerations

- Review for hard-coded values after initialization
- Ensure sensitive outputs marked appropriately
- Verify IAM follows least privilege
- Check for public access configurations

## Tests & Examples

> Unknown - requires submodule initialization

Expected structure:
```
terraform-azurerm-postgresqlflexibleserver/
├── examples/
│   └── simple/
│       └── main.tf
└── tests/
    └── module_test.go
```

## Migration Notes

If this module uses deprecated variable names, migrate using:

```hcl
# Old (deprecated)
env = "dev"
proj = "myapp"

# New (canonical)
environment = "dev"
project = "myapp"
```

## Changelog

> Cannot determine version history - submodule not initialized

---

*Generated by Cloud 2.0 Terraform Module Audit - 2025-11-25*
*Regenerate after submodule initialization for complete documentation*
